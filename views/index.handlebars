<h1>Eat-Da-Burger</h1>

<h2>Burgers dying to be eaten!</h2>

<ul>
    {{#each burger}}
        {{#unless devoured}}
            {{> burger/burger-devour devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers already eaten!</h2>

<ul>
    {{#each burger}}
        {{#if devoured}}
            {{> burger/burger-devour devoured=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burgerName">Burger Name:</label>
        <input type="text" id="user_burgerName" name="name">
    </div>

    <button type="submit">Add Burger!</button>

</form>
